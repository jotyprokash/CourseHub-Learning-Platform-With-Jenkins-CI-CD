apiVersion: v1
kind: Secret
metadata:
  name: {{ include "coursehub.fullname" . }}-secrets
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  POSTGRES_PASSWORD: {{ .Values.database.password | b64enc | quote }}
  JWT_SECRET: {{ "change-me" | b64enc | quote }}
